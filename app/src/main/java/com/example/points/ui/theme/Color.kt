package com.example.points.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de marca POINTS - Modo Claro (Inspirada en Airbnb)
val PointsPrimary = Color(0xFFE31C5F)      // Rojo salmón/coral característico de Airbnb
val PointsSecondary = Color(0xFF00A699)    // Verde teal suave
val PointsTertiary = Color(0xFF767676)     // Gris neutro
val PointsAccent = Color(0xFFE31C5F)       // Mismo rojo salmón para acentos
val PointsError = Color(0xFFE31C5F)        // Rojo salmón para errores
val PointsSuccess = Color(0xFF00A699)      // Verde teal para éxito

// Colores neutros - Modo Claro (Estilo Airbnb)
val PointsInk = Color(0xFF222222)          // Negro suave para texto principal
val PointsSubtle = Color(0xFF717171)       // Gris medio para texto secundario
val PointsSurface = Color(0xFFFFFFFF)      // Blanco puro para superficies
val PointsOutline = Color(0xFFDDDDDD)      // Borde muy sutil

// Paleta de marca POINTS - Modo Oscuro (Inspirada en Airbnb)
val PointsPrimaryDark = Color(0xFFE31C5F)  // Mismo rojo salmón para consistencia
val PointsSecondaryDark = Color(0xFF00A699) // Verde teal suave
val PointsTertiaryDark = Color(0xFFB0B0B0)  // Gris claro para modo oscuro
val PointsAccentDark = Color(0xFFE31C5F)    // Mismo rojo salmón

// Colores neutros - Modo Oscuro (Estilo Airbnb)
val PointsSurfaceDark = Color(0xFF121212)  // Negro suave para superficies
val PointsOnSurfaceDark = Color(0xFFFFFFFF) // Blanco para texto sobre superficie oscura
val PointsSurfaceVariantDark = Color(0xFF1E1E1E) // Variante de superficie oscura
val PointsOutlineDark = Color(0xFF333333)  // Borde oscuro sutil

// Colores de estado para incidentes (Estilo Airbnb)
val StatusPending = Color(0xFFFF9500)      // Naranja suave - Pendiente
val StatusInReview = Color(0xFF007AFF)     // Azul iOS - En revisión
val StatusConfirmed = Color(0xFF00A699)    // Verde teal - Confirmado
val StatusRejected = Color(0xFFE31C5F)     // Rojo salmón - Rechazado
val StatusResolved = Color(0xFF00A699)     // Verde teal - Resuelto

// Colores de prioridad (Estilo Airbnb)
val PriorityLow = Color(0xFF00A699)        // Verde teal - Baja
val PriorityMedium = Color(0xFFFF9500)     // Naranja suave - Media
val PriorityHigh = Color(0xFFE31C5F)       // Rojo salmón - Alta
val PriorityCritical = Color(0xFFCC0000)   // Rojo intenso - Crítica

// Colores de categoría (Estilo Airbnb)
val CategoryInfrastructure = Color(0xFF007AFF)  // Azul iOS - Infraestructura
val CategoryEnvironment = Color(0xFF00A699)     // Verde teal - Medio ambiente
val CategorySafety = Color(0xFFE31C5F)          // Rojo salmón - Seguridad
val CategoryTransport = Color(0xFF8E44AD)       // Púrpura suave - Transporte
val CategoryOther = Color(0xFF717171)           // Gris neutro - Otros

// Colores de mapa (Estilo Airbnb)
val MapMarkerDefault = Color(0xFFE31C5F)   // Rojo salmón - Marcador por defecto
val MapMarkerAlert = Color(0xFFFF9500)     // Naranja suave - Marcador de alerta
val MapCluster = Color(0xFF007AFF)         // Azul iOS - Cluster
val MapUserLocation = Color(0xFF00A699)    // Verde teal - Ubicación del usuario

// Colores de feedback (Estilo Airbnb)
val FeedbackInfo = Color(0xFF007AFF)       // Azul iOS - Información
val FeedbackWarning = Color(0xFFFF9500)    // Naranja suave - Advertencia
val FeedbackError = Color(0xFFE31C5F)      // Rojo salmón - Error
val FeedbackSuccess = Color(0xFF00A699)    // Verde teal - Éxito

// Colores de carga (Estilo Airbnb)
val ShimmerBase = Color(0xFFF5F5F5)        // Base del shimmer más suave
val ShimmerHighlight = Color(0xFFFFFFFF)   // Resaltado del shimmer blanco

// Colores de transparencia (Estilo Airbnb)
val OverlayLight = Color(0x40000000)       // Overlay más sutil
val OverlayDark = Color(0x40000000)        // Overlay más sutil
val Scrim = Color(0x40000000)              // Scrim más sutil para modales

// Colores legacy (mantener para compatibilidad)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)